# ============================================
# 懶得變有錢部落格 - 私人分支專用 .gitignore
# ============================================
# 此檔案用於 private-dev 分支，允許同步開發資料到其他裝置

# 系統檔案 (仍需忽略)
Thumbs.db
.DS_Store
.dist
.tmp
.lock
.sass-cache

# Node.js (仍需忽略大型檔案)
npm-debug.log
node_modules/
package-lock.json
yarn.lock

# Hugo 建置檔案 (仍需忽略)
public/
sitemap.xml
resources/
.hugo_build.lock
jsconfig.json
hugo_stats.json

# Go 模組
go.sum

# ============================================
# 🔓 允許同步的開發檔案 (僅在私人分支)
# ============================================

# 以下檔案在私人分支中允許同步:
# - AI 記憶檔案系統
# - 開發工具設定
# - 日誌檔案
# - 環境配置

# ============================================
# 🚫 絕對不同步的敏感檔案
# ============================================

# 環境變數 (包含 API 密鑰)
.env
.env.local
.env.production
.env.secret

# 個人認證
*.key
*.pem
*.p12
.ssh/
auth.json
credentials.json

# 作業系統快取
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db

# 大型壓縮檔案
*.zip
*.tar.gz
*.rar
reindex.zip

# 嵌入的 git repository
Claude-Code-Usage-Monitor/
