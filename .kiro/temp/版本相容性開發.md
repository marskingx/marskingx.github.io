好的，收到。將這些具體的檔案和資料夾路徑加入到 Prompt 中，是一個非常好的做法。這能為 Kiro 提供更豐富、更精準的上下文，讓它規劃出來的「規格書」更加貼近您專案的實際情況。

我會將您提供的這段資訊，無縫地整合到我們剛才設計好的 Prompt 中。

---

### **【開發任務提示 (v2.1)：規劃「AI 版控工具 Docker 兼容性」升級任務】**

您可以直接複製以下**更新後**的完整內容，貼給 Kiro。

```
# 指令：規劃「AI 版控工具 Docker 兼容性」升級任務

[你的角色]
你是 Kiro，本專案的「AI 開發團隊技術主管」。你的任務是為我規劃一項系統升級任務，確保我們現有的自動化工具能在新的 Docker 開發容器中無縫運作。

[情境上下文]
1.  **現況**: 我們已經成功將整個專案的開發環境，遷移到一個基於 Docker 的 Dev Container 中。
2.  **問題**: 我們引以為傲的 AI 自動化版控腳本（例如 `smart-git-manager.js` 和 `smart-git-pull.js`）目前無法在容器中工作。原因是它們內部寫死了 Windows 的絕對路徑（例如 `D:\...`）來存取私有記憶庫。
3.  **目標**: 我們需要將這些腳本升級，使其具備「環境感知」能力。它們需要能夠判斷自己是否在容器內，並使用正確的路徑存取私有記憶庫，同時也要保留在 Windows 環境下直接運行的能力（向下相容）。

**[關鍵的私有檔案與目錄]**
根據 `scripts/smart-git-manager.js` 的定義，我們的 AI 版控系統高度依賴以下私有檔案與目錄。這些都是需要在新舊環境中都能被正確存取的目標：
* `.kiro/`
* `docs/aimemory/`
* `.claude-backups/`
* `.claude/`
* `Claude-Code-Usage-Monitor/`
* `.ai-lock.json`
* `scripts/aimemory-log-update.js`
* `scripts/version-manager.js`

[技術方案方向]
我建議採用以下方案，請你基於此方向規劃具體實施細節：
1.  **容器配置 (`.devcontainer/devcontainer.json`)**:
    * 使用 `mounts` 功能，將 Windows 上的私有庫資料夾，以「綁定掛載」的方式，掛載到容器內部的一個指定路徑（例如 `/sync` 或 `/private-repo`）。
    * 使用 `containerEnv` 功能，在容器內設定一個環境變數（例如 `PRIVATE_REPO_PATH`），其值就是上面設定的掛載路徑。
2.  **腳本修改 (針對 `smart-git-manager.js` 及相關檔案)**:
    * 修改腳本，使其不再寫死路徑。
    * 腳本啟動時，應優先嘗試讀取 `PRIVATE_REPO_PATH` 這個環境變數。
    * 如果讀取成功，就使用此路徑；如果讀取失敗（代表不在容器環境中），則使用原始的 Windows 路徑作為後備。

[你的任務]
請你根據以上所有資訊，為我產出一份完整的**「開發規格書 (Development Spec)」**。這份規格書需要足夠清晰，以便我可以直接交給 Claude 或 Codex 進行實作。規格書必須包含以下章節：

1.  **目標 (Objective)**: 簡述本次升級要達成的最終目標。
2.  **技術方案 (Technical Solution)**: 詳細描述我們將採用的 `mounts` 和 `containerEnv` 技術。
3.  **詳細的檔案修改步驟 (Detailed File Modification Steps)**:
    * **針對 `.devcontainer/devcontainer.json`**: 提供需要新增的 `mounts` 和 `containerEnv` 完整程式碼區塊。
    * **針對 `smart-git-manager.js`**: 提供如何修改路徑設定的具體程式碼範例（展示如何讀取環境變數並設定後備路徑）。
    * **針對 `smart-git-pull.js`**: 同上，提供修改建議。
4.  **驗證步驟 (Verification Steps)**: 描述在容器內和容器外，我們應該如何測試以確保功能升級成功。

請開始撰寫這份規格書。
```
